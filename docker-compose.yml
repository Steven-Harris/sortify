services:
  sortify:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sortify
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./media:/media:rw
    environment:
      - SORTIFY_PORT=8080
      - SORTIFY_MEDIA_PATH=/media
      - SORTIFY_CORS_ORIGINS=http://localhost:8080
